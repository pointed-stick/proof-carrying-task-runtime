"""Plugins: domain-specific tools, verifiers, and context renderers.

Built-in:
  code_tests   — write Python files, run pytest, verify by re-running pytest

Stubs (porting targets):
  knowledge_qa — port from ../decompose_graph_agent.py (triples + wiki + evidence)
"""
