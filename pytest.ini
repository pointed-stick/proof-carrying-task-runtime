[pytest]
# Only collect from the runtime's own test directory; never recurse into the
# demo-generated workspaces (which contain their own tests/test_slugify.py
# fixtures and would collide on module names) or any cache directory.
testpaths = task_runtime/tests
norecursedirs =
    __pycache__
    .pytest_cache
    *_workspace
    wiki_cache
